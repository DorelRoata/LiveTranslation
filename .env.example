# Rename this file to .env.local and add your API key below
# so you don't have to paste it into the UI every time.
# .env.local is ignored by git so your key will be safe.

VITE_GEMINI_API_KEY=your_api_key_here
