# API keys are not loaded from Vite environment files.
# Enter the key in the localhost dashboard on first launch. LiveTranslation
# stores it in the current user's application settings outside this repository.
